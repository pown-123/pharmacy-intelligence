import { Boxes } from "lucide-react";

import { ComingNextPage } from "@/components/app/coming-next-page";

export default function InventoryPage() {
  return (
    <ComingNextPage
      title="Inventory"
      description="Route placeholder for inventory visibility across branches."
      icon={Boxes}
    />
  );
}
